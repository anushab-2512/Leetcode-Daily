class Solution(object):
    def toLowerCase(self, s):
        """
        :type s: str
        :rtype: str
        """   
        res=s.lower()
        return res
        
# Time: O(n) — checks each character.
# Space: O(n) — a new lowercase string is created.
